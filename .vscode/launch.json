{
    "version": "0.2.0",
    "configurations": [
        

        {
            "name": "Run Server",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/server.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [
                {
                    "name": "PATH",
                    "value": "C:/Program Files/MySQL/MySQL Server 8.0/lib;${env:PATH}"
                }
            ],
            "externalConsole": true,
            "MIMode": "gdb",
            "miDebuggerPath": "gdb.exe",
            "preLaunchTask": "build"
        }
    ]
}